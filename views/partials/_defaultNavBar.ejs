<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-logged-in" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/" class="navbar-brand">Smash Tracker</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-logged-in">
      <ul class="nav navbar-nav navbar-right">
        <% if(req.user) { %>
          <li><a href="/user/<%= req.user.id %>/edit">Edit Profile</a></li>
          <li><a href="/logout">Logout</a></li>
          <li><p class="navbar-text">Signed in as <a href="/u/<%= req.user.username %>" class="navbar-link"><%= req.user.username %></a></p></li>
        <% } else { %>
          <li><a href="#" data-toggle="modal" data-target="#login-modal">Login/Register</a></li>
        <% } %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>